{% extends 'base.html.twig' %}

{% block title %}Medecin{% endblock %}

{% block body %}

<div class="container">
    <h1>Medecin</h1>
    <table class="table">
        <tbody>


            <tr>
                <th>Nom</th>
                <td>{{ medecin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ medecin.prenom }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ medecin.cin }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ medecin.age }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ medecin.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ medecin.telephone }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ medecin.address }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ medecin.specialite }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ medecin.status }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ medecin.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="dispo" href="{{ path('dispo_ah_indexA', {id: medecin.id}) }}" type="button" data-id="{{ medecin.id }}" >Disponibilité</a>
    <a href="{{ path('medecin_index') }}">back to list</a>
</div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="{{ asset('assets/js/router.js') }}"></script>

    <script src="{{ asset('assets/js/fos_js_routes.js') }}"></script>



    <script>
        url_momo = Routing.generate('momo');
        $(document).ready(function () {
            $('.dispo').on('click', function () {
                var id = $(this).attr("data-id")
                var medId =$(".medId").val(id)
                $.ajax({
                    url: url_momo,
                    type: "POST",
                    data:{
                        medId: medId,
                    },

                    cache: false,
                    success: function (data) {

                    },
                    error: function (error) {
                    }
                })

            });

        });

    </script>

{% endblock %}
